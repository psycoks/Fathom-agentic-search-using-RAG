import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Next.js 16 no longer runs ESLint as part of `next build` (the `eslint`
   * config key was removed), so there's nothing to configure here for that.
   * Run `npm run lint` separately if you want lint feedback. */
};

export default nextConfig;
